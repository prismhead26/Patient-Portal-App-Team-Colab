
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js'></script>
        <script>
          var user = JSON.parse(decodeURI("{{data}}"))
          const num = 0
          const appointments = `{{this.appointments.[${num}].time}}`
          user['appointments'].forEach((element) => console.log('time ...',element.time));
          user['patients'].forEach((element) => console.log('patients ...',element.name));
          console.log('user', user['appointments'])
          console.log('name', appointments)
      function createCalendar() {
        document.addEventListener('DOMContentLoaded', function() {
          var calendarEl = document.getElementById('calendar');
          var calendar = new FullCalendar.Calendar(calendarEl, {
            initialView: 'dayGridMonth',
          });
          calendar.render();
        });
      }
      createCalendar()

    </script>
  </head>
  <body>
    <div id='calendar'></div>
  </body>